# NewLife.NovaDb 需求规格说明书

## 1. 产品概述
*   **全称**：NewLife.NovaDb
*   **简称**：Nova
*   **定位**：以 **C#** 实现、运行于 **.NET 8** 平台的中大型混合数据库，支持嵌入式/服务器双模，融合关系型、时序、消息队列、NoSQL(KV) 能力。
*   **愿景**：NewLife 生态核心基础设施，替代 SQLite/MySQL/Redis/TDengine 在特定场景的使用。
*   **约束**：纯托管代码（C#/.NET），不依赖 Native 组件。
*   **目标**：通过裁剪大量冷门能力（如存储过程/触发器/窗口函数等），换取更高的读写性能与更低的运维成本；数据量逻辑上无上限（受磁盘与切分策略约束）。

## 2. 核心架构

| 模块 | 规格说明 | 设计意图 |
| :--- | :--- | :--- |
| **部署模式** | **嵌入模式** (本地文件夹) / **服务器模式** (独立进程 TCP，支持集群与主从同步) | 嵌入如 SQLite，网络如 MySQL |
| **文件管理** | **文件夹即数据库**，每表独立文件组 (`.data`, `.idx`, `.wal`) | 拷贝文件夹即完成迁移/备份 |
| **存储介质** | **MMF (内存映射文件)** 为主，物理内存为辅 | 突破物理内存限制，支持超大表 |
| **索引策略** | **动态冷热分离** (见 3.1 节) | 热点快，冷点省内存，支持 5 亿行 |
| **WAL 日志** | **FULL** (同步) / **NORMAL** (异步 1s) / **NONE** (异步) | 全局配置，默认 NORMAL |
| **通信协议** | 自定义二进制协议 (无压缩) | 极致性能，ADO.NET 驱动自动识别 |

## 3. 存储引擎详解

### 3.1 默认引擎：Nova Engine (通用关系型)
*   **数据结构**：SkipList (跳表)。
*   **容量目标**：单表支持 **无限行**（上限取决于磁盘空间与文件切分策略）。
*   **内存优化 (核心)**：
    *   **热数据 (Hot)**：最近访问 (如最近 10 分钟) 的数据段，索引完整加载至物理内存 (SkipList 节点)。
    *   **冷数据 (Cold)**：长期未访问数据，索引卸载至 MMF，内存仅保留**稀疏目录** (如每 1 万行一个锚点)。
    *   **效果**：1000 万行表，若只查最新 1 万行，内存占用 < 20MB；若全表随机查，依赖 MMF 页 fault，性能下降但可用。
    *   **淘汰机制**：LRU 策略，超过阈值 (如 30 分钟未访问) 自动卸载热索引。
*   **持久化与切分 (核心)**：
    *   **单表无上限**：逻辑上单表不设行数上限，数据增长主要消耗磁盘资源。
    *   **默认文件布局**：每表独立文件组（`.data`, `.idx`, `.wal`）。
    *   **自动文件切分**：当单个物理文件达到阈值（按大小/时间/行数等策略）后，自动切分为多个数据分片（Shard），以支持无上限存储。
    *   **冷热加载原则**：表数据极少读取或不读取时，不应占用显著内存；只有频繁读取的热点范围，才提升为内存热索引。
*   **索引文件 (核心)**：
    *   **多索引多文件**：一张表每个索引（主键/二级索引/唯一索引等）对应独立索引文件（例如 `PK.idx`, `{IndexName}.idx`），便于独立加载、淘汰与重建。
    *   **idx 不是内存跳表节点镜像**：`.idx` 存储可持久化的“目录 + 块/页”结构（稀疏目录锚点 -> 块偏移），用于按需 MMF 映射与快速定位；内存 SkipList 用作热缓存结构，可从 `.idx` 按需重建热点区域。
*   **适用场景**：通用 CRUD，配置表，业务订单，用户数据。

### 3.2 时序引擎：Flux Engine (时序 + MQ)
*   **数据结构**：按时间分片 (如每天/每小时一个文件)，追加写 (Append Only)。
*   **容量目标**：单表支持 **无限行** (依赖磁盘空间)。
*   **MQ 功能 (Stream)**：
    *   **模型**：参考 Redis Stream 的消费组模型（`Topic/Stream` + `ConsumerGroup` + `Consumer` + `Pending`）。
    *   **消息 ID**：时间戳 + 序列（同毫秒内自增），整体单调递增。
    *   **可靠性**：At-Least-Once，读取后进入 Pending，业务成功后 Ack。
    *   **阻塞读取**：支持阻塞 + 超时（类似 `XREADGROUP BLOCK`）。
    *   **可观测性**：系统表记录队列、消费组、消费者、Pending 等状态，便于 SQL 查询使用情况。
*   **数据保留**：支持 TTL (按时间/文件大小自动删除旧分片)。
*   **适用场景**：物联网传感器数据，日志收集，内部消息队列，审计日志。

### 3.3 KV 模式 (逻辑视图)
*   **实现**：特殊 Schema 的 Nova 表 (`Key` string PK, `Value` byte[])。
*   **接口**：
    *   SQL: `SELECT Value FROM KvTable WHERE Key = '...'`
    *   API: `db.KV.Get("key")`, `db.KV.Set("key", data)`
*   **适用场景**：分布式锁，缓存，会话存储，配置中心。

## 4. 数据类型与函数

| 类别 | 支持内容 | 备注 |
| :--- | :--- | :--- |
| **基础类型** | `bool`, `int`, `long`, `double`, `decimal` (128-bit), `string` (UTF-8), `byte[]` | 严格映射 C# 类型 |
| **时间类型** | `datetime` (内部存 Ticks) | 支持时序排序 |
| **时间函数** | `NOW`, `DATEDIFF`, `DATE_ADD`, `YEAR`, `MONTH`, `DAY`, `HOUR` | 满足基础统计 |
| **约束** | 主键 (唯一), 索引 (可唯一) | **无外键**，无 Check 约束 |

## 5. SQL 与功能支持

| 功能 | 支持度 | 说明 |
| :--- | :--- | :--- |
| **DDL/DML** | ✅ 高 | Create/Alter/Drop, Insert/Update/Delete/Upsert |
| **查询** | ✅ 中 | Select/Where/Order/Group By |
| **连接** | ⚠️ 基础 | **Join** (Nested Loop)，未来支持 **Hash Join**；**Subquery** (IN/Exists) |
| **事务** | ✅ 高 | MVCC, Read Committed, Commit/Rollback |
| **MQ 订阅** | ✅ 新增 | `CREATE CONSUMER`, `COMMIT OFFSET` (扩展语法/API) |
| **NoSQL** | ✅ 新增 | KV 表专用 API 支持（每行 TTL：`Key + Value + TTL`），支持分布式部署 |
| **高级** | ❌ 无 | 无视图/触发器/存储过程 |

## 6. 主流数据库对比分析

### 6.1 关系型数据库对比

| 特性 | SQLite | MySQL | PostgreSQL | SQL Server | **NovaDb** |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **实现语言** | C | C/C++ | C | C/C++ | **C#** |
| **开源协议** | Public Domain | GPL v2 | PostgreSQL License | 商业 | **MIT** |
| **运行形态** | 嵌入（库） | 独立服务 | 独立服务 | 独立服务 | **嵌入（库）/ 服务（进程）** |
| **部署方式** | 单文件 | 安装+配置 | 安装+配置 | 安装+配置 | **文件夹即数据库，零配置** |
| **存储结构** | B+ 树 / 单文件 | InnoDB 页/段 | 页/段 (堆表) | 页/区 | **表文件组（分片）+ MMF** |
| **索引结构** | B+ 树 (磁盘) | B+ 树 (磁盘) | B+ 树 / GIN / GiST | B+ 树 / 列存储 | **SkipList (内存+MMF 冷热分离)** |
| **内存管理** | 固定缓存池 | Buffer Pool (LRU) | 共享内存 (Buffer) | Buffer Pool | **动态冷热分离 (热段内存+冷段 MMF)** |
| **事务隔离** | Serializable | RC / RR / Serializable | RC / RR / Serializable | RC / SI / Serializable | **Read Committed (MVCC)** |
| **并发模型** | 单写多读 (WAL) | 多写多读 (MVCC) | 多写多读 (MVCC) | 多写多读 (锁/MVCC) | **多写多读 (MVCC)** |
| **迁移/备份** | 拷贝文件 | mysqldump / 主从 | pg_dump / 流复制 | bak 文件 / AlwaysOn | **拷贝文件夹** |
| **扩展能力** | 纯关系型 | 纯关系型 | 关系型+JSON | 关系型+JSON+图 | **关系型 + 时序 + MQ + KV** |
| **运维复杂度** | 极低 | 中 | 中 | 中偏高 | **低（单文件夹/单进程）** |
| **跨平台** | ✅ | ✅ | ✅ | Windows 为主 | **✅ (.NET 8 跨平台)** |
| **适用规模** | 小型/嵌入 | 中大型 | 中大型 | 中大型 | **中大型（逻辑上无上限）** |
| **生态集成** | 通用 ADO | 通用 ADO | 通用 ADO | 通用 ADO | **NewLife XCode 原生集成** |

### 6.2 SQL92 标准实现度对比

| SQL 能力 | SQLite | MySQL | PostgreSQL | SQL Server | **NovaDb** | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **CREATE / ALTER / DROP** | ✅ | ✅ | ✅ | ✅ | **✅** | NovaDb 支持基础 DDL |
| **INSERT / UPDATE / DELETE** | ✅ | ✅ | ✅ | ✅ | **✅** | |
| **UPSERT** | ✅ (REPLACE) | ✅ (ON DUPLICATE KEY) | ✅ (ON CONFLICT) | ✅ (MERGE) | **✅ (Upsert)** | NovaDb 原生 Upsert |
| **SELECT / WHERE / ORDER BY** | ✅ | ✅ | ✅ | ✅ | **✅** | |
| **GROUP BY / HAVING** | ✅ | ✅ | ✅ | ✅ | **✅** | |
| **聚合函数 (COUNT/SUM/AVG/MIN/MAX)** | ✅ | ✅ | ✅ | ✅ | **✅** | |
| **JOIN (INNER/LEFT/RIGHT)** | ✅ | ✅ | ✅ | ✅ | **⚠️ 基础 (Nested Loop)** | 初期不支持 Hash Join（规划支持） |
| **子查询 (IN/EXISTS)** | ✅ | ✅ | ✅ | ✅ | **⚠️ 基础** | |
| **CTE (WITH 递归)** | ✅ | ✅ (8.0+) | ✅ | ✅ | **❌** | |
| **窗口函数 (ROW_NUMBER 等)** | ✅ (3.25+) | ✅ (8.0+) | ✅ | ✅ | **❌** | |
| **视图 (VIEW)** | ✅ | ✅ | ✅ | ✅ | **❌** | |
| **外键约束** | ✅ (可选) | ✅ | ✅ | ✅ | **❌** | 不支持外键 |
| **CHECK 约束** | ✅ | ✅ (8.0.16+) | ✅ | ✅ | **❌** | |
| **触发器** | ✅ | ✅ | ✅ | ✅ | **❌** | |
| **存储过程/函数** | ❌ | ✅ | ✅ | ✅ | **❌** | |
| **事务 COMMIT/ROLLBACK** | ✅ | ✅ | ✅ | ✅ | **✅ (MVCC)** | Read Committed |
| **Serializable 隔离** | ✅ | ✅ | ✅ | ✅ | **❌** | 仅 Read Committed |
| **MERGE INTO** | ❌ | ❌ | ❌ (用 ON CONFLICT) | ✅ | **❌ (用 Upsert 替代)** | |
| **GRANT/REVOKE (权限)** | ❌ | ✅ | ✅ | ✅ | **❌ (v1 不实现)** | |
| **预估覆盖率** | ~85% | ~95% | ~98% | ~98% | **~60%** | 覆盖业务常用子集 |

> 说明：NovaDb 的 SQL 目标是"覆盖业务常用子集 + 可扩展"，不追求完整 SQL92。高级功能（CTE、窗口函数、触发器等）可在后续版本按需迭代。

### 6.3 时序数据库对比

| 特性 | InfluxDB | TDengine | TimescaleDB | QuestDB | **NovaDb (Flux Engine)** |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **实现语言** | Go (v2) / Rust (v3) | C | C (PG 扩展) | Java | **C#** |
| **开源协议** | MIT (v2) / Apache 2.0 (v3) | AGPL v3 | Apache 2.0 (社区版) | Apache 2.0 | **MIT** |
| **部署方式** | 独立服务 | 独立服务/集群 | PG 扩展 | 独立服务 | **嵌入 / 独立服务** |
| **数据模型** | Tag + Field + Timestamp | 超级表 (STable) | 普通表 + 时间分区 | 表 + 时间列 | **时间分片表 (按天/小时)** |
| **写入模式** | Line Protocol (追加) | 追加写 | SQL INSERT (追加) | 追加写 | **追加写 (Append Only)** |
| **存储引擎** | TSM / IOx (列存储) | VNode 分片 | 基于 PG 堆表 + 分区 | 列存储 (零 GC) | **时间分片文件 + WAL + MMF** |
| **查询语言** | Flux / InfluxQL / SQL (v3) | 类 SQL (TQL) | 标准 SQL | 标准 SQL | **SQL (扩展语法)** |
| **降采样/聚合** | 连续查询 (Flux) | 内置 INTERVAL | TimescaleDB Toolkit | 内置采样 | **计划支持（优先级较低）** |
| **数据保留 (TTL)** | ✅ (Retention Policy) | ✅ | ✅ | ✅ | **✅ (按时间/大小自动删除)** |
| **压缩** | Gorilla + RLE | 列压缩 | PG TOAST | 列压缩 | **✅（冷数据分片压缩）** |
| **集群/分布式** | ✅ (企业版) | ✅ (原生) | ❌ (靠 PG 方案) | ✅ (企业版) | **✅（集群 + 主从同步）** |
| **MQ 能力** | ❌ | ❌ | ❌ | ❌ | **✅ (内置 Stream/MQ)** |
| **运维复杂度** | 中 | 中偏高 | 低 (PG 生态) | 低 | **极低（文件夹即库）** |
| **适用规模** | 中大型 | 大型/超大型 | 中大型 | 中大型 | **小中型 / 嵌入** |
| **.NET 集成** | HTTP SDK | 原生驱动 | Npgsql | HTTP / PGWire | **原生嵌入 + ADO.NET** |

### 6.4 NoSQL 数据库对比

| 特性 | Redis | MongoDB | LevelDB / RocksDB | etcd | **NovaDb (KV 模式)** |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **实现语言** | C | C++ | C++ | Go | **C#** |
| **数据模型** | KV + 多种数据结构 | 文档 (BSON) | 有序 KV | KV (强一致) | **KV 表 (Key-Value)** |
| **部署方式** | 独立服务 / 集群 | 独立服务 / 副本集 | 嵌入式库 | 独立集群 (3/5 节点) | **嵌入 / 独立服务** |
| **持久化** | RDB / AOF (可选) | WiredTiger (默认持久) | WAL + SSTable | WAL + Snapshot | **WAL + MMF (强持久化)** |
| **事务** | MULTI/EXEC (弱事务) | 多文档事务 (4.0+) | 批量写 (原子) | Mini 事务 (Revision) | **MVCC 事务 (Read Committed)** |
| **查询能力** | 命令式 (无 SQL) | 丰富查询 + 聚合管道 | Get/Put/Scan | Range + Watch | **SQL 查询 + API** |
| **内存模型** | 全内存为主 | 工作集缓存 (WiredTiger) | 块缓存 (Block Cache) | 全内存 (BoltDB) | **冷热分离 (热段内存+冷段 MMF)** |
| **数据结构** | String/Hash/List/Set/ZSet/Stream | 文档/数组/嵌套 | 有序字节 KV | 扁平 KV | **Key + Value + TTL（每行）** |
| **TTL 支持** | ✅ (Key 级别) | ✅ (TTL 索引) | ❌ (需自实现) | ✅ (Lease) | **✅（每行 TTL）** |
| **发布订阅** | ✅ (Pub/Sub + Stream) | ✅ (Change Stream) | ❌ | ✅ (Watch) | **✅ (Flux Engine MQ)** |
| **分布式** | Cluster / Sentinel | 副本集 / 分片 | ❌ (嵌入式) | Raft 共识 (强一致) | **✅（集群）** |
| **运维复杂度** | 低 | 中 | 极低 (嵌入) | 中 | **极低（文件夹即库）** |
| **适用场景** | 缓存/会话/排行榜/队列 | 文档存储/内容管理 | 嵌入式存储/LSM 底层 | 配置中心/服务发现 | **缓存/会话/配置/分布式锁** |
| **.NET 集成** | StackExchange.Redis | MongoDB.Driver | 需 P/Invoke | dotnet-etcd | **原生嵌入 + ADO.NET** |

### 6.5 消息队列对比

| 特性 | Kafka | RabbitMQ | Redis Stream | RocketMQ | Pulsar | **NovaDb (Flux MQ)** |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **实现语言** | Java/Scala | Erlang | C | Java | Java | **C#** |
| **部署复杂度** | 高 (ZK/KRaft + Broker) | 中 (Erlang 运行时) | 低 (Redis 实例) | 中 (NameServer + Broker) | 高 (ZK + BK + Broker) | **极低（嵌入或单进程）** |
| **消息模型** | 分区 + 消费组 (Pull) | Exchange + Queue (Push/Pull) | Stream + 消费组 (Pull) | Topic + Queue (Pull) | Topic + 订阅 (Pull/Push) | **Stream + 消费组 (Pull)** |
| **消息持久化** | ✅ 磁盘 (顺序写) | ✅ 磁盘 (可选) | ✅ (AOF/RDB，可选) | ✅ 磁盘 (CommitLog) | ✅ 磁盘 (BookKeeper) | **✅ WAL + 时间分片文件** |
| **消息可靠性** | At-Least-Once / Exactly-Once | At-Least-Once / At-Most-Once | At-Least-Once | At-Least-Once | At-Least-Once / Exactly-Once | **At-Least-Once** |
| **消息顺序** | 分区内有序 | 单队列有序 | 全局有序 | 分区内有序 | 分区内有序 | **全局有序 (单 Stream)** |
| **消息回溯** | ✅ (按 Offset) | ❌ | ✅ (按 ID/时间) | ✅ (按时间) | ✅ (按 ID/时间) | **✅ (按 ID/时间)** |
| **延迟消息** | ❌ (需扩展) | ✅ (TTL + DLX) | ❌ | ✅ (内置) | ✅ (内置) | **❌ (v1 不支持)** |
| **死信队列** | ❌ (需自建) | ✅ (DLX) | ❌ | ✅ (内置) | ✅ (内置) | **❌ (v1 不支持)** |
| **阻塞读取** | ✅ (长轮询) | ✅ (Push 模式) | ✅ (XREADGROUP BLOCK) | ✅ (长轮询) | ✅ (Push/长轮询) | **✅ (阻塞 + 超时)** |
| **消息 TTL/保留** | ✅ (按时间/大小) | ✅ (Per-Message TTL) | ✅ (MAXLEN/MINID) | ✅ (按时间) | ✅ (按时间/大小) | **✅ (按时间/大小自动删除)** |
| **吞吐量** | 极高 (百万/s) | 中 (万级/s) | 高 (十万/s) | 高 (十万/s) | 极高 (百万/s) | **中等（受 .NET GC 与单机限制）** |
| **SQL 查询消息** | ❌ (KSQL 独立组件) | ❌ | ❌ | ❌ | ✅ (Pulsar SQL) | **✅ (原生 SQL 查询)** |
| **运维可观测** | Kafka Manager / JMX | Management UI | INFO / MONITOR | Dashboard | Admin API | **系统表 SQL 查询** |
| **集群** | ✅ (原生) | ✅ (镜像队列/仲裁队列) | ✅ (Redis Cluster) | ✅ (原生) | ✅ (原生) | **✅（集群 + 主从同步）** |
| **.NET 集成** | Confluent.Kafka | RabbitMQ.Client | StackExchange.Redis | rocketmq-client-csharp | DotPulsar | **原生嵌入 + ADO.NET** |
| **适用场景** | 大数据管道/事件溯源 | 企业集成/任务分发 | 轻量级内部队列 | 电商/金融 | 多租户/云原生 | **嵌入式内部队列 / IoT 消息** |

### 6.6 NovaDb 综合差异化总结

| 差异化能力 | 说明 |
| :--- | :--- |
| **纯 .NET 8 托管** | 无 Native 依赖，部署即 xcopy，与 .NET 应用同进程零序列化开销 |
| **嵌入+服务双模** | 开发调试嵌入如 SQLite，生产部署独立服务如 MySQL，同一套 API |
| **集群与主从同步** | 独立服务支持集群部署，支持主从同步与一主多从，用于读扩展与容灾 |
| **文件夹即数据库** | 拷贝文件夹完成迁移/备份，无需 dump/restore 流程 |
| **冷热分离索引** | 1000 万行表仅查热点时内存 < 20MB，冷数据自动卸载至 MMF |
| **关系型 + 时序 + MQ + KV 融合** | 单一引擎覆盖 SQLite + TDengine + Redis 常见场景，减少组件数 |
| **NewLife XCode 原生集成** | ORM 层直接适配，无需第三方驱动 |

## 7. 关键设计决策确认

1.  **索引冷热分离**：
    *   **机制**：内存维护“热段 SkipList" + “冷段稀疏目录”。
    *   **触发**：基于访问时间 (LRU)，后台线程定期卸载冷索引。
    *   **目标**：1000 万行表，仅查最新 1 万行时，内存占用 < 20MB。
2.  **超大表支持**：
*   **上限**：逻辑上无限制（受磁盘空间与文件切分策略约束）。
*   **代价**：冷数据查询性能随数据量与命中分片范围下降（依赖 MMF 页 fault），可能出现秒级响应。
    *   **建议**：XCode 层仍建议对超大数据进行逻辑分表 (按时间/Hash)。
3.  **MQ 可靠性**：
    *   **模型**：At-Least-Once (至少一次)。
    *   **流程**：读取消息 -> 进入 Pending -> 业务处理 -> Ack/确认（服务端更新消费组偏移量并移除 Pending）。
    *   **风险**：崩溃重试可能导致重复消费，需业务层幂等。
    *   **参考**：整体模型参考 Redis Stream 的消费组语义（服务端维护每个消费组的消费进度与 Pending）。
4.  **KV 实现**：
    *   不单独建引擎，复用 Nova Engine，通过 API 屏蔽 SQL 细节。

---
